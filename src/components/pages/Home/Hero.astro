<section class="hero relative flex flex-col items-center text-center gap-4 max-w-[var(--measure-content-max)] mx-auto mb-8" aria-labelledby="home-title">
  <img
    src="/imgs/profile.jpg"
    alt="Portrait of Santiago Carrasco"
    width="130"
    height="130"
    class="rounded-full border border-line-strong shadow-2 object-cover"
  />

  <p class="m-0 text-sm leading-tight tracking-[0.03em] text-ink-muted">Santiago Carrasco (Santychuy)</p>
  <h1 id="home-title" class="m-0 max-w-[20ch] leading-tight text-hero text-wrap-balance text-ink">Software engineer building practical AI experiences</h1>
  <p class="m-0 max-w-[var(--reading-max-width)] text-ink-muted text-lg leading-copy-compact text-wrap-pretty">
    I write about AI agents, modern web development, and what I learn while
    building useful products.
  </p>

  <div class="mt-3 flex gap-3 flex-wrap justify-center">
    <a class="inline-flex items-center justify-center touch-target rounded-pill py-2 px-5 font-semibold text-sm leading-tight bg-brand text-surface-up border border-transparent transition-[transform,border-color,background-color,color] duration-base ease-standard hover-lift hover:bg-brand focus-ring" href="#latest-posts">Read latest posts</a>
    <a class="inline-flex items-center justify-center touch-target rounded-pill py-2 px-5 font-semibold text-sm leading-tight border border-line-strong bg-surface-dim text-ink transition-[transform,border-color,background-color,color] duration-base ease-standard hover-lift hover:border-brand focus-ring" href="#contact-me">Get in touch</a>
  </div>
</section>

<style>
  .hero::after {
    content: '';
    position: absolute;
    inset-inline: 15%;
    inset-block-start: calc(var(--space-7) * -1);
    height: var(--measure-hero-glow-height);
    background: radial-gradient(
      ellipse,
      var(--paper-accent-soft) 0%,
      transparent 70%
    );
    z-index: -1;
  }
</style>
